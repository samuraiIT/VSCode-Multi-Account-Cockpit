# PR-Ready Summary — 2026-05-03

## Title

`security: harden trust boundaries and finalize verification`

## What changed

- enforced OAuth callback `state` validation and loopback-only callback binding
- restricted privileged webview actions to explicit command and URL policy
- removed raw-HTML rendering from sync stats markdown flows
- validated imported account IDs before shared storage writes
- constrained local sync file operations to their storage root
- required trusted chat-id binding for username-configured Telegram auth
- validated Google Drive names/query literals before Drive API lookup
- allowlisted Cloud Code override hosts before bearer-token requests
- constrained local conversation file opens to expected roots
- scoped TypeScript compilation to `src/**/*.ts`
- synchronized repository types so `tsc --noEmit` passes

## Validation

- `npx tsc --noEmit --pretty false` — passed
- focused ESLint on touched security files — warnings only, no errors
- manual source/sink review completed for each validated finding and deferred shard boundary

## Commits

- `cb0d5d3`
- `808add7`
- `d562ae0`

Note:
- `cb0d5d3` contains the code hardening changes, but its commit message is mislabeled because an earlier parallel commit attempt raced on the git index lock.

## Remaining repo state

- unrelated user/local modifications still exist outside this change set and were intentionally preserved
