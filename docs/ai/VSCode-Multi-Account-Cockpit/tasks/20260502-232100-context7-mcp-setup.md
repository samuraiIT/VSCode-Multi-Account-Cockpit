# Task 20260502-232100-context7-mcp-setup

## User intent

Connect MCP according to `C:\Диск D\!project_Windows\docs\mcp` and continue the full code audit using `Context7` when possible.

## Execution prompt

```text
Use the canonical MCP rules from docs/mcp/MCP_RemoteSSH_Ubuntu_Auto_Setup.md.

Goals:
- connect Context7 to Codex with backup and rollback
- avoid tokens in config.toml
- prefer low-load remote HTTP/OAuth path over ad-hoc local wrappers
- verify the resulting MCP registration
- continue the repository audit and capture real findings only
```

## Working assumptions

- In this Windows-side Codex session, the correct place for the Codex MCP configuration is `C:\Users\rooot\.codex\config.toml`.
- `Context7` can be connected through Codex CLI directly and should not be hand-edited into TOML without backup.
- The current running agent session may need a fresh start to see newly added MCP servers.

## Result

- `Context7` was added successfully via:
  - `codex mcp add context7 --url https://mcp.context7.com/mcp`
- OAuth flow completed successfully.
- `codex mcp list` and `codex mcp get context7` confirmed the configuration.
- The current developer-tool runtime did not hot-reload the new server, so this turn's audit still used local analysis plus sub-agent review.
- The follow-up repository audit produced concrete findings around import safety, backup cleanup scope, proxy process killing, terminal secret exposure, and MCP autostart config drift.
